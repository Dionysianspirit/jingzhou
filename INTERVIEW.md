# 径舟 · 面试口述稿

把项目讲成「我解决过的检索问题」，不要讲成「我接了个模型」。

## 30 秒定位

径舟是一个面向中文教材的 RAG 学习助手。用户把多份资料投进书斋，系统用 BGE 中文向量做检索，再让大模型只根据命中片段回答，并生成闪卡、测验和学习指南。产品对标 IMA / NotebookLM 的「资料在先、回答必须带出处」，工程上刻意把切块、相似度、落盘和引用回查拆开，方便讲原理。

## 3 分钟主线（建议按这个顺序）

### 1. 切块不是「按字数一刀切」

教材有短定义也有长推导。先按空行分段，能装进 2048 字的就拼在一起；单段仍然超长时，在 2048 以内找最近的句号切开，并回带 512 字重叠。短于 200 字的尾巴并回上一块，避免出现几乎没有语义的碎片向量。

可现场打开 `app/chunking.py`，跑：

```bash
python -m unittest tests.test_chunking -v
```

### 2. 检索分必须可解释

嵌入模型是 `BAAI/bge-small-zh-v1.5`，输出已 L2 归一化，所以 `score = chunk · query` 就是余弦。接口 `/api/search` 和聊天来源里会带：

- `score`：和问句的余弦
- `rank`：在候选里的名次
- `overlap`：问句与原文共享的字词（辅助人工核对，不是打分项）
- `why`：一句话说明为什么留下这块

点来源笺会请求 `/api/docs/{id}/chunks/{i}`，看到的就是当时送进提示词的原文，而不是模型事后编的摘要。

### 3. 生成被检索管住

系统提示要求：没有命中就承认「遍览全卷，未有所获」；引用写成 `[来源: 文档名 片段N]`。测验还要求每个错项归入「概念混淆 / 公式遗忘 / 审题偏移 / 推导断裂」四类。

学习指南走同一套检索，只是查询改成「导读 核心概念 易错点 思考题」，再要求模型吐结构化 JSON。

## 可能被追问

**为什么不用 BM25？**  可以作混合检索的第一路。当前版本先把中文语义做完整，再用 overlap 给人看字面重合。

**向量存在哪？**  进程内是 `dict[id] -> ndarray`，磁盘是 `data/store/<id>/embeddings.npy`。重启后 `load_disk()` 读回。

**幻觉怎么压？**  检索 top-k 才进上下文；提示词禁止外部知识；引用可回查。

## 演示脚本

1. 启动后点「载入示例教材」。
2. 问「什么时候矩阵可以对角化？」
3. 打开检索笺，对照分数和原文。
4. 点「指南」。
5. 关掉终端再开一次，文档还在。
